package com.mealam.showdown.loader.cache.dragons;

public record StatsCache(
		int attack,
		int speed,
		int armor,
		int firepower,
		int shotLimit,
		int venom,
		int jawStrength,
		int stealth
) {
}
